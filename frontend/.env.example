# Copy to .env.local and fill in for your deployment. Both are public URLs.
# Defaults (in src/config.ts) assume `wrangler dev` on :8787.

VITE_WORKER_URL=https://velorouter-worker.your-subdomain.workers.dev
VITE_PMTILES_URL=https://your-r2-public-host/network.pmtiles
